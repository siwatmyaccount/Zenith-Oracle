# ============================================================
# ⚠️ ไฟล์นี้ป้องกัน API Key ไม่ให้ขึ้น GitHub โดยไม่ตั้งใจ
# ============================================================

# ซ่อนไฟล์ตั้งค่า API Key
config.js

# ไฟล์ระบบ
.DS_Store
Thumbs.db
