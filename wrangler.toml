# ============================================================
# wrangler.toml — ไฟล์ตั้งค่า Cloudflare Worker
# วางไว้ที่โฟลเดอร์ worker/ แยกจากโปรเจกต์หลัก
# ============================================================

name = "zenith-oracle-proxy"
main = "worker.js"
compatibility_date = "2024-01-01"
